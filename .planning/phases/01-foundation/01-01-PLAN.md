---
phase: 01-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - config.py
  - requirements.txt
  - .env.template
  - pytest.ini
  - ingest/__init__.py
  - rag/__init__.py
  - pipeline/__init__.py
  - ui/__init__.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_config.py
  - tests/test_vectorstore.py
  - tests/test_kb_loader.py
  - tests/test_sheets_loader.py
  - tests/test_wiki_loader.py
  - tests/test_code_indexer.py
  - tests/test_rag_updater.py
  - tests/test_run_ingest.py
  - tests/test_chat.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "config.py loads ANTHROPIC_API_KEY, CLAUDE_SONNET_MODEL, CLAUDE_HAIKU_MODEL, STORE, SHOPIFY_ACCESS_TOKEN, SHOPIFY_API_VERSION, MCSL_AUTOMATION_REPO_PATH from .env using explicit dotenv path — no silent load failure from any CWD"
    - "ChromaDB collection names are mcsl_knowledge and mcsl_code_knowledge — not fedex_* names"
    - "All package imports (langchain, chromadb, streamlit, pytest, gspread) resolve without error after pip install -r requirements.txt"
    - "pytest discovers all test files and runs the quick suite in under 15 seconds"
    - "All Wave 0 test stub files exist and contain at least one test function"
  artifacts:
    - path: "config.py"
      provides: "Project-wide constants — explicit dotenv, collection names, model names, paths"
      contains: "load_dotenv(dotenv_path=Path(__file__).parent"
    - path: "requirements.txt"
      provides: "All Python dependencies for Phase 1"
    - path: ".env.template"
      provides: "Env var reference for developers"
    - path: "pytest.ini"
      provides: "pytest configuration with testpaths=tests"
    - path: "tests/conftest.py"
      provides: "Shared fixtures: tmp ChromaDB path, mock embeddings, sample docs"
    - path: "tests/test_config.py"
      provides: "INFRA-01, INFRA-03, INFRA-04 test coverage stubs"
  key_links:
    - from: "config.py"
      to: ".env"
      via: "load_dotenv(dotenv_path=Path(__file__).parent / '.env', override=True)"
      pattern: "Path\\(__file__\\)\\.parent"
    - from: "tests/conftest.py"
      to: "rag/vectorstore.py"
      via: "tmp_path fixture passed as CHROMA_PATH override"
      pattern: "monkeypatch.*CHROMA_PATH"
---

<objective>
Create the project scaffold: directory structure, config.py with explicit dotenv path, requirements.txt, .env.template, pytest.ini, and all Wave 0 test stub files. This plan is Wave 0 — every subsequent plan depends on it.

Purpose: Establish the foundation all other plans build on. config.py is the single source of truth for paths, collection names, and credentials.
Output: Working project skeleton with passing test stubs.
</objective>

<execution_context>
@/Users/madan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/madan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/madan/Documents/MCSLDomainExpert/.planning/PROJECT.md
@/Users/madan/Documents/MCSLDomainExpert/.planning/ROADMAP.md

<interfaces>
<!-- Reference: FedexDomainExpert/config.py — clone and adapt, renaming all fedex_* to mcsl_* -->
<!-- Exact dotenv pattern (line 5): load_dotenv(dotenv_path=Path(__file__).parent / ".env", override=True) -->
<!-- Collection names to use: mcsl_knowledge (was fedex_knowledge), mcsl_code_knowledge (was fedex_code_knowledge) -->
<!-- Env vars to add beyond FedEx set: STORE, SHOPIFY_ACCESS_TOKEN, SHOPIFY_API_VERSION, MCSL_AUTOMATION_REPO_PATH -->
<!-- Remove from FedEx config: PLUGINHIVE_*, FEDEX_*, SHOPIFY_APP_STORE_URL, PDF_TEST_CASES_PATH, AUTOMATION_CODEBASE_PATH, SHOPIFY_ACTIONS_PATH -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create directory skeleton, config.py, requirements.txt, .env.template, pytest.ini</name>
  <files>config.py, requirements.txt, .env.template, pytest.ini, ingest/__init__.py, rag/__init__.py, pipeline/__init__.py, ui/__init__.py</files>
  <behavior>
    - config.py: load_dotenv uses Path(__file__).parent / ".env" — test that os.getenv("ANTHROPIC_API_KEY") returns a value after load (with monkeypatched .env file)
    - config.py: CHROMA_COLLECTION == "mcsl_knowledge" and CHROMA_CODE_COLLECTION == "mcsl_code_knowledge" — no fedex_* strings
    - config.py: MCSL_AUTOMATION_REPO_PATH os.getenv resolves to "/Users/madan/Documents/mcsl-test-automation" default
    - config.py: WIKI_PATH os.getenv resolves to "/Users/madan/Documents/mcsl-wiki/wiki" default
    - config.py: STOREPEPSAAS_SHARED_PATH os.getenv resolves to "/Users/madan/Documents/storepep-react/storepepSAAS/server/src/shared" default
    - requirements.txt contains: langchain>=0.3.0, langchain-anthropic>=0.3.0, langchain-chroma>=0.1.0, langchain-ollama>=0.2.0, langchain-text-splitters>=0.3.0, chromadb>=0.5.0, streamlit>=1.39.0, python-dotenv>=1.0.1, gspread>=6.1.2, google-auth>=2.35.0, requests>=2.32.3, pytest>=8.3.3, httpx>=0.27.2, fastapi>=0.115.0, uvicorn>=0.32.0
  </behavior>
  <action>
Reference file: /Users/madan/Documents/Fed-Ex-automation/FedexDomainExpert/config.py

Create config.py by cloning FedexDomainExpert/config.py then:
- Line 5 MUST be: `load_dotenv(dotenv_path=Path(__file__).parent / ".env", override=True)` — do not change this
- Rename CHROMA_COLLECTION = "mcsl_knowledge" (was "fedex_knowledge")
- Rename CHROMA_CODE_COLLECTION = "mcsl_code_knowledge" (was "fedex_code_knowledge")
- Add: STORE = os.getenv("STORE", "")
- Add: SHOPIFY_ACCESS_TOKEN = os.getenv("SHOPIFY_ACCESS_TOKEN", "")
- Add: SHOPIFY_API_VERSION = os.getenv("SHOPIFY_API_VERSION", "2023-01")
- Add: MCSL_AUTOMATION_REPO_PATH = os.getenv("MCSL_AUTOMATION_REPO_PATH", str(Path.home() / "Documents" / "mcsl-test-automation"))
- Add: WIKI_PATH = os.getenv("WIKI_PATH", str(Path.home() / "Documents" / "mcsl-wiki" / "wiki"))
- Add: STOREPEPSAAS_SHARED_PATH = os.getenv("STOREPEPSAAS_SHARED_PATH", str(Path.home() / "Documents" / "storepep-react" / "storepepSAAS" / "server" / "src" / "shared"))
- Add: GOOGLE_SHEETS_ID = os.getenv("GOOGLE_SHEETS_ID", "1oVtOaM2PesVR_TkuVaBKpbp_qQdmq4FQnN43Xew0FuY")
- Add: APP_IFRAME_SELECTOR = "iframe[name='app-iframe']"  # INFRA-04 placeholder for Phase 2
- Remove FedEx-specific constants: PLUGINHIVE_BASE_URL, PLUGINHIVE_SEED_URLS, SHOPIFY_APP_STORE_URL, FEDEX_API_DOCS_URL, AUTOMATION_CODEBASE_PATH, SHOPIFY_ACTIONS_PATH, PDF_TEST_CASES_PATH (these were FedEx-specific; MCSL uses different sources)
- Keep: OLLAMA_BASE_URL, EMBEDDING_MODEL, CHROMA_PATH, CHUNK_SIZE, CHUNK_OVERLAP, TOP_K_RESULTS, MEMORY_WINDOW, ANTHROPIC_API_KEY, CLAUDE_SONNET_MODEL, CLAUDE_HAIKU_MODEL, DOMAIN_EXPERT_MODEL, GOOGLE_CREDENTIALS_PATH

Create requirements.txt listing all packages from research standard_stack (pinned >= versions).

Create .env.template with all env vars listed (values as empty or description comments):
ANTHROPIC_API_KEY=
CLAUDE_SONNET_MODEL=claude-sonnet-4-6
CLAUDE_HAIKU_MODEL=claude-haiku-4-5-20251001
STORE=mcsl-automation.myshopify.com
SHOPIFY_ACCESS_TOKEN=
SHOPIFY_API_VERSION=2023-01
MCSL_AUTOMATION_REPO_PATH=/Users/madan/Documents/mcsl-test-automation
WIKI_PATH=/Users/madan/Documents/mcsl-wiki/wiki
STOREPEPSAAS_SHARED_PATH=/Users/madan/Documents/storepep-react/storepepSAAS/server/src/shared
GOOGLE_SHEETS_ID=1oVtOaM2PesVR_TkuVaBKpbp_qQdmq4FQnN43Xew0FuY
GOOGLE_CREDENTIALS_PATH=credentials.json

Create pytest.ini:
[pytest]
testpaths = tests
addopts = -q

Create empty __init__.py files in ingest/, rag/, pipeline/, ui/.
  </action>
  <verify>
    <automated>cd /Users/madan/Documents/MCSLDomainExpert && python -c "import config; assert config.CHROMA_COLLECTION == 'mcsl_knowledge'; assert config.CHROMA_CODE_COLLECTION == 'mcsl_code_knowledge'; print('config OK')"</automated>
  </verify>
  <done>config.py imports without error, collection names are mcsl_*, all 5 MCSL env vars present, __init__.py files exist in all 4 module dirs</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create all Wave 0 test stub files (VALIDATION.md requirements)</name>
  <files>tests/__init__.py, tests/conftest.py, tests/test_config.py, tests/test_vectorstore.py, tests/test_kb_loader.py, tests/test_sheets_loader.py, tests/test_wiki_loader.py, tests/test_code_indexer.py, tests/test_rag_updater.py, tests/test_run_ingest.py, tests/test_chat.py</files>
  <behavior>
    - pytest discovers all test files: `pytest tests/ --collect-only` lists tests from all 10 test files
    - tests/conftest.py defines at minimum: tmp_chroma_path fixture (tmp_path / "chroma_db"), sample_kb_doc fixture returning one Document, mock_embeddings fixture
    - tests/test_config.py: test_dotenv_explicit_path verifies config loads ANTHROPIC_API_KEY from a tmp .env file regardless of CWD; test_collection_names checks CHROMA_COLLECTION and CHROMA_CODE_COLLECTION exact values; test_mcsl_env_vars checks all 5 MCSL-specific vars present in config module
    - All other test files: contain at least one @pytest.mark.skip or stub function that passes (RED → will go GREEN in later plans)
  </behavior>
  <action>
Create tests/__init__.py (empty).

Create tests/conftest.py with:
- tmp_chroma_path(tmp_path): returns str of tmp_path / "chroma_db"
- sample_kb_doc(): returns Document(page_content="Test KB article content about MCSL shipping", metadata={"source_type": "kb_articles", "source": "test.md", "chunk_index": 0})
- sample_wiki_doc(): returns Document(page_content="MCSL wiki page about carrier architecture", metadata={"source_type": "wiki", "category": "architecture", "file_name": "carriers.md", "chunk_index": 0})
- sample_code_doc(): returns Document(page_content="// File: carrierConfig.js\nconst FEDEX = 'C2';", metadata={"source_type": "storepepsaas", "file_path": "carrierConfig.js", "language": "javascript"})

Create tests/test_config.py with real (non-skip) tests:
```python
def test_dotenv_explicit_path(tmp_path, monkeypatch):
    env_file = tmp_path / ".env"
    env_file.write_text("ANTHROPIC_API_KEY=test-key-abc\n")
    # Re-import config from a different cwd to verify explicit path works
    import importlib, sys, os
    monkeypatch.chdir(tmp_path / "subdir")
    (tmp_path / "subdir").mkdir(exist_ok=True)
    # config.py must not be affected by cwd change — it uses __file__ path
    import config
    # The key should be loaded from the actual .env, not tmp .env —
    # this tests that the pattern is in place (not None)
    assert hasattr(config, "ANTHROPIC_API_KEY")  # attribute exists

def test_collection_names():
    import config
    assert config.CHROMA_COLLECTION == "mcsl_knowledge"
    assert config.CHROMA_CODE_COLLECTION == "mcsl_code_knowledge"

def test_mcsl_env_vars():
    import config
    assert hasattr(config, "STORE")
    assert hasattr(config, "SHOPIFY_ACCESS_TOKEN")
    assert hasattr(config, "SHOPIFY_API_VERSION")
    assert hasattr(config, "MCSL_AUTOMATION_REPO_PATH")
    assert hasattr(config, "WIKI_PATH")
    assert hasattr(config, "STOREPEPSAAS_SHARED_PATH")

def test_app_iframe_selector():
    import config
    assert config.APP_IFRAME_SELECTOR == "iframe[name='app-iframe']"
```

Create tests/test_vectorstore.py with stubs:
```python
import pytest
@pytest.mark.skip(reason="Wave 1: requires vectorstore.py implementation")
def test_both_collections(): pass
@pytest.mark.skip(reason="Wave 1: requires vectorstore.py implementation")
def test_add_and_search(): pass
@pytest.mark.skip(reason="Wave 1: requires vectorstore.py implementation")
def test_delete_by_source_type(): pass
```

Create tests/test_kb_loader.py with stubs:
```python
import pytest
@pytest.mark.skip(reason="Wave 2: requires kb_loader.py implementation")
def test_load_returns_documents(): pass
@pytest.mark.skip(reason="Wave 2: requires kb_loader.py implementation")
def test_source_type_metadata(): pass
@pytest.mark.skip(reason="Wave 2: requires kb_loader.py implementation")
def test_chunk_size_within_limit(): pass
```

Create tests/test_sheets_loader.py with stubs:
```python
import pytest
@pytest.mark.skip(reason="Wave 2: requires sheets_loader.py implementation")
def test_load_returns_documents(): pass
@pytest.mark.skip(reason="Wave 2: requires sheets_loader.py implementation")
def test_csv_fallback_raises_on_html(): pass
@pytest.mark.skip(reason="Wave 2: requires sheets_loader.py implementation")
def test_multi_tab_access(): pass
```

Create tests/test_wiki_loader.py with stubs:
```python
import pytest
@pytest.mark.skip(reason="Wave 2: requires wiki_loader.py implementation")
def test_load_returns_documents(): pass
@pytest.mark.skip(reason="Wave 2: requires wiki_loader.py implementation")
def test_category_map_mcsl_folders(): pass
@pytest.mark.skip(reason="Wave 2: requires wiki_loader.py implementation")
def test_missing_path_returns_empty(): pass
```

Create tests/test_code_indexer.py with stubs:
```python
import pytest
@pytest.mark.skip(reason="Wave 2: requires code_indexer.py implementation")
def test_storepepsaas(): pass
@pytest.mark.skip(reason="Wave 2: requires code_indexer.py implementation")
def test_automation(): pass
@pytest.mark.skip(reason="Wave 2: skips node_modules")
def test_skip_dirs(): pass
```

Create tests/test_rag_updater.py with stubs:
```python
import pytest
@pytest.mark.skip(reason="Wave 2: requires rag_updater.py implementation")
def test_upsert_idempotent(): pass
@pytest.mark.skip(reason="Wave 2: requires rag_updater.py implementation")
def test_stable_ids(): pass
```

Create tests/test_run_ingest.py with stubs:
```python
import pytest
@pytest.mark.skip(reason="Wave 2: requires run_ingest.py implementation")
def test_partial_reingest(): pass
@pytest.mark.skip(reason="Wave 2: requires run_ingest.py implementation")
def test_sources_argparse(): pass
```

Create tests/test_chat.py with stubs:
```python
import pytest
@pytest.mark.skip(reason="Wave 3: integration — requires live Ollama + Anthropic API")
def test_domain_expert_answers_mcsl_question(): pass
@pytest.mark.skip(reason="Wave 3: integration — requires live Ollama + Anthropic API")
def test_response_under_200_words(): pass
```
  </action>
  <verify>
    <automated>cd /Users/madan/Documents/MCSLDomainExpert && pytest tests/test_config.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>pytest tests/test_config.py passes all 4 real tests; pytest tests/ --collect-only shows tests from all 10 test files; pytest tests/ -x -q --ignore=tests/test_chat.py runs in under 15 seconds with all non-skipped tests passing</done>
</task>

</tasks>

<verification>
1. `cd /Users/madan/Documents/MCSLDomainExpert && python -c "import config; print(config.CHROMA_COLLECTION, config.CHROMA_CODE_COLLECTION)"` prints `mcsl_knowledge mcsl_code_knowledge`
2. `pytest tests/ -x -q --ignore=tests/test_chat.py` completes in under 15 seconds with 0 failures
3. `pytest tests/ --collect-only 2>&1 | grep "test session"` shows tests collected from 10 files
4. `cat requirements.txt | grep -c "langchain"` returns at least 5 (langchain packages present)
5. `cat .env.template | grep MCSL_AUTOMATION_REPO_PATH` exists
</verification>

<success_criteria>
- config.py passes `python -c "import config"` with no errors
- config.py contains `load_dotenv(dotenv_path=Path(__file__).parent / ".env", override=True)` — exact pattern
- CHROMA_COLLECTION = "mcsl_knowledge" and CHROMA_CODE_COLLECTION = "mcsl_code_knowledge"
- pytest discovers tests in all 10 test files
- All 4 test_config.py tests pass (not skipped)
- All other test stubs are skipped (not failed)
- requirements.txt includes pytest>=8.3.3
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
