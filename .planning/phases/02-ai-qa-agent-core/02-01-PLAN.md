---
phase: 02-ai-qa-agent-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_agent.py
  - pipeline/smart_ac_verifier.py
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-03

must_haves:
  truths:
    - "Given raw AC text, the agent extracts a JSON array of testable scenario strings without error"
    - "For each scenario, the agent queries the domain expert and receives an insight string under 200 words"
    - "The agent produces a JSON execution plan with keys: nav_clicks, look_for, api_to_watch, order_action, carrier"
    - "Carrier name detected from AC text is injected into the planning prompt (e.g. FedEx AC → carrier: FedEx in plan)"
  artifacts:
    - path: "tests/test_agent.py"
      provides: "Pytest stubs for all AGENT-01..07 and CARRIER-01 unit tests (all initially skipped)"
      exports: ["test_extract_scenarios", "test_domain_expert_query", "test_build_execution_plan",
                "test_browser_loop_scaffold", "test_ax_tree_capture", "test_action_handlers",
                "test_carrier_detection", "test_order_creator", "test_verdict_reporting"]
    - path: "pipeline/smart_ac_verifier.py"
      provides: "Five-stage scenario pipeline scaffold with _extract_scenarios, _ask_domain_expert, _plan_scenario, verify_ac"
      contains: "_EXTRACT_PROMPT, _PLAN_PROMPT, _MCSL_WORKFLOW_GUIDE, _MCSL_URL_MAP, CARRIER_CODES"
  key_links:
    - from: "pipeline/smart_ac_verifier.py"
      to: "rag/vectorstore.py"
      via: "_ask_domain_expert() queries mcsl_knowledge + mcsl_code_knowledge"
      pattern: "mcsl_knowledge|mcsl_code_knowledge"
    - from: "pipeline/smart_ac_verifier.py"
      to: "config.py"
      via: "STORE, SHOPIFY_ACCESS_TOKEN, ANTHROPIC_API_KEY loaded via config"
      pattern: "import config"
---

<objective>
Create the Wave 0 test stubs and the smart_ac_verifier.py scaffold: scenario extractor, domain expert query, planning prompt with carrier injection, and the MCSL-specific constants (URL map, workflow guide, carrier codes).

Purpose: Every subsequent plan in this phase depends on the data models and function signatures defined here. Wave 0 stubs enable pytest to run green from the first commit so feedback sampling is live.
Output: tests/test_agent.py with 9 skipped stubs; pipeline/smart_ac_verifier.py with _extract_scenarios, _ask_domain_expert, _plan_scenario, verify_ac entry point, and all MCSL constants.
</objective>

<execution_context>
@/Users/madan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/madan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ai-qa-agent-core/02-RESEARCH.md

Reference implementation to READ before writing MCSL version:
/Users/madan/Documents/Fed-Ex-automation/FedexDomainExpert/pipeline/smart_ac_verifier.py

Key architectural decisions:
- Clone/adapt FedexDomainExpert smart_ac_verifier.py — do NOT rewrite from scratch
- App slug: mcsl-qa (NOT testing-553)
- iframe selector: iframe[name="app-iframe"]
- MCSL label flow uses app order grid, NOT Shopify More Actions
- Python venv: .venv/ — all commands: PYTHONPATH=. .venv/bin/python or .venv/bin/pytest
- auth.json path: /Users/madan/Documents/mcsl-test-automation/auth.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Wave 0): Create tests/test_agent.py with all stubs skipped</name>
  <files>tests/test_agent.py</files>
  <behavior>
    - test_extract_scenarios: pytest.skip stub, imports _extract_scenarios from pipeline.smart_ac_verifier
    - test_domain_expert_query: pytest.skip stub, imports _ask_domain_expert
    - test_build_execution_plan: pytest.skip stub, imports _plan_scenario
    - test_browser_loop_scaffold: pytest.skip stub, imports _verify_scenario
    - test_ax_tree_capture: pytest.skip stub, imports _ax_tree
    - test_action_handlers: pytest.skip stub, imports _do_action
    - test_carrier_detection: pytest.skip stub, imports _detect_carrier from pipeline.smart_ac_verifier
    - test_order_creator: pytest.skip stub, imports create_order from pipeline.order_creator
    - test_verdict_reporting: pytest.skip stub, imports VerificationReport, ScenarioResult
    - All stubs must pass (skipped = pass in pytest) immediately: PYTHONPATH=. .venv/bin/pytest tests/test_agent.py -q exits 0
  </behavior>
  <action>
Read the existing tests/conftest.py for fixture patterns, then create tests/test_agent.py. Each test function should have a docstring stating its requirement ID and use pytest.mark.skip(reason="Wave 0 stub — implement after plan task completes"). Import each symbol at function body level (not module level) to avoid import errors before the symbols exist. Use `from pipeline.smart_ac_verifier import ...` and `from pipeline.order_creator import ...` inside the test body under a try/except ImportError that also skips.

Pattern for each stub:
```python
@pytest.mark.skip(reason="Wave 0 stub — AGENT-01")
def test_extract_scenarios():
    """AGENT-01: Agent extracts testable scenarios from AC text as JSON array."""
    from pipeline.smart_ac_verifier import _extract_scenarios
    result = _extract_scenarios("Given a UPS account is configured, when...", claude=None)
    assert isinstance(result, list)
    assert len(result) > 0
```
  </action>
  <verify>
    <automated>PYTHONPATH=. .venv/bin/pytest tests/test_agent.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>All 9 stubs collected and skipped, exit code 0, no import errors at module level</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Create pipeline/smart_ac_verifier.py scaffold — scenario extractor, domain expert query, planning prompt</name>
  <files>pipeline/smart_ac_verifier.py</files>
  <behavior>
    - test_extract_scenarios: _extract_scenarios(ac_text, claude) returns list[str] with at least one element when ac_text is non-empty
    - test_domain_expert_query: _ask_domain_expert(scenario, card_name, claude) returns a non-empty string
    - test_build_execution_plan: _plan_scenario(scenario, app_url, code_ctx, expert_insight, claude) returns dict with keys nav_clicks, look_for, api_to_watch, order_action, carrier
    - test_carrier_detection: _detect_carrier("FedEx account") returns ("FedEx", "C2"); _detect_carrier("UPS label") returns ("UPS", "C3"); _detect_carrier("unknown") returns ("", "")
  </behavior>
  <action>
READ /Users/madan/Documents/Fed-Ex-automation/FedexDomainExpert/pipeline/smart_ac_verifier.py first (lines 1-400 to understand imports, dataclasses, constants, _EXTRACT_PROMPT, _PLAN_PROMPT structure).

Then create pipeline/smart_ac_verifier.py with:

1. **Imports** — port exactly from FedexDomainExpert: base64, json, logging, os, re, tempfile, zipfile, dataclasses, pathlib, textwrap, typing, langchain_anthropic, langchain_core.messages, config. Add `from __future__ import annotations`.

2. **Constants**:
   - `MAX_STEPS = 15`
   - `_ANTI_BOT_ARGS` — port from FedEx (includes --disable-blink-features=AutomationControlled and channel="chrome")
   - `_AUTH_JSON = Path("/Users/madan/Documents/mcsl-test-automation/auth.json")`
   - `CARRIER_CODES = {"fedex": ("FedEx", "C2"), "ups": ("UPS", "C3"), "dhl": ("DHL", "C1"), "usps": ("USPS", "C22"), "stamps": ("USPS Stamps", "C22"), "easypost": ("EasyPost", "C22"), "canada post": ("Canada Post", "C4")}`

3. **`_detect_carrier(ac_text: str) -> tuple[str, str]`** — iterate CARRIER_CODES keys in order, return (name, code) on first match in ac_text.lower(), default ("", "").

4. **`_MCSL_URL_MAP` builder** — function `_build_url_map(app_base: str, store: str) -> dict[str, str]` returning:
   ```python
   {
     "shipping":    f"{app_base}/shopify",
     "appproducts": f"{app_base}/products",
     "settings":    f"{app_base}/settings/0",
     "carriers":    f"{app_base}/settings/0",
     "pickup":      f"{app_base}/pickup",
     "faq":         f"{app_base}/faq",
     "rates log":   f"{app_base}/rateslog",
     "orders":      f"https://admin.shopify.com/store/{store}/orders",
     "shopifyproducts": f"https://admin.shopify.com/store/{store}/products",
   }
   ```
   App base URL pattern: `https://admin.shopify.com/store/{store}/apps/mcsl-qa`

5. **`_MCSL_WORKFLOW_GUIDE`** — multi-line string describing MCSL-specific flows. Include:
   - Label generation: Click 'ORDERS' tab → All Orders grid loads inside iframe → Add filter: Order Id → paste ID → press Escape → click the order link (bold order number) → Order Summary page opens → (optional) if 'Prepare Shipment' button visible click it (retry up to 3x) → Click 'Generate Label' button (exact match, inside app iframe) → wait for order status to reach 'LABEL CREATED' (up to 800s)
   - Rate log: View all Rate Summary → 3-dots → View Log → .dialogHalfDivParent
   - Bulk labels: Order Grid → header checkbox → Generate labels button → Label Batch page
   - Carrier account config: App Settings → Carriers → Add/Edit carrier
   - iframe: all app UI is inside iframe[name="app-iframe"]
   - Status locator: div[class="order-summary-greyBlock"] > div:nth-child(1) > div:nth-child(1) > div > span
   - DO NOT use Shopify More Actions for label generation (MCSL-specific)

6. **Dataclasses** — port from FedexDomainExpert: `StepResult`, `ScenarioResult`, `VerificationReport`. Add `carrier: str = ""` field to `ScenarioResult`.

7. **`_EXTRACT_PROMPT`** — port from FedexDomainExpert (this prompt is carrier-neutral, minor MCSL text adaptation only).

8. **`_PLAN_PROMPT`** — port from FedexDomainExpert and add carrier injection block:
   ```
   CARRIER: {carrier_name} (internal code: {carrier_code})
   Use carrier-specific navigation, service codes, and account config flows.
   Refer to _MCSL_WORKFLOW_GUIDE for MCSL-specific navigation paths.
   ```
   Plan schema must include `carrier` field in addition to nav_clicks, look_for, api_to_watch, order_action.

9. **`_parse_json(text: str) -> Any`** — port verbatim from FedexDomainExpert (handles markdown fences, prefix text).

10. **`_extract_scenarios(ac_text: str, claude) -> list[str]`** — port from FedexDomainExpert _extract_scenarios(). Uses `_EXTRACT_PROMPT`, calls claude with HumanMessage, parses JSON list.

11. **`_ask_domain_expert(scenario: str, card_name: str, claude) -> str`** — port from FedexDomainExpert. Queries `mcsl_knowledge` and `mcsl_code_knowledge` ChromaDB collections (import from rag.vectorstore). Returns insight string ≤200 words.

12. **`_code_context(scenario: str, card_name: str) -> str`** — port from FedexDomainExpert, queries `mcsl_code_knowledge`.

13. **`_plan_scenario(scenario: str, app_url: str, code_ctx: str, expert_insight: str, claude) -> dict`** — port from FedexDomainExpert. Call `_detect_carrier()` on the scenario text, inject carrier into prompt, invoke claude, parse JSON plan dict.

14. **`verify_ac(ac_text: str, card_name: str, stop_flag: Callable | None = None) -> VerificationReport`** — stub entry point. Initialise ChatAnthropic from config, call `_extract_scenarios()`, loop with stop_flag check, stub `_verify_scenario()` call (returns ScenarioResult with status="qa_needed" until later plans). Return VerificationReport.

Note: `_verify_scenario`, `_ax_tree`, `_screenshot`, `_network`, `_do_action`, `_decide_next` are stubs returning safe defaults — they will be filled in by plans 02-02 and 02-03.

After writing, unskip test_extract_scenarios, test_domain_expert_query, test_build_execution_plan, test_carrier_detection in tests/test_agent.py — replace pytest.mark.skip with real assertions. Use mock claude (MagicMock) that returns a pre-baked JSON string in these unit tests.
  </action>
  <verify>
    <automated>PYTHONPATH=. .venv/bin/pytest tests/test_agent.py::test_extract_scenarios tests/test_agent.py::test_domain_expert_query tests/test_agent.py::test_build_execution_plan tests/test_agent.py::test_carrier_detection -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>4 tests pass (not skipped), _detect_carrier("FedEx account") returns ("FedEx", "C2"), _plan_scenario returns dict with carrier key, verify_ac runs without raising on empty AC text</done>
</task>

</tasks>

<verification>
PYTHONPATH=. .venv/bin/pytest tests/test_agent.py -q
- All 9 tests collected, 4 pass (unskipped), 5 skipped, 0 failed
- python -c "from pipeline.smart_ac_verifier import verify_ac, _detect_carrier, CARRIER_CODES; print('OK')" exits 0
- python -c "from pipeline.smart_ac_verifier import _MCSL_WORKFLOW_GUIDE; assert 'mcsl-qa' in _MCSL_WORKFLOW_GUIDE.lower() or 'ORDERS' in _MCSL_WORKFLOW_GUIDE; print('OK')"
</verification>

<success_criteria>
- tests/test_agent.py exists with 9 stubs, PYTHONPATH=. .venv/bin/pytest tests/test_agent.py -q exits 0
- pipeline/smart_ac_verifier.py imports cleanly, _detect_carrier works for all 4 major carriers
- _plan_scenario returns a dict with carrier field injected from AC text
- _MCSL_URL_MAP uses mcsl-qa app slug (not testing-553)
- All dataclasses (StepResult, ScenarioResult, VerificationReport) are importable
</success_criteria>

<output>
After completion, create .planning/phases/02-ai-qa-agent-core/02-01-SUMMARY.md
</output>
