---
phase: 03-label-docs-pre-requirements
plan: "02"
type: tdd
wave: 2
depends_on:
  - 03-01
files_modified:
  - pipeline/smart_ac_verifier.py
  - tests/test_label_flows.py
autonomous: true
requirements:
  - LABEL-02
  - LABEL-04

must_haves:
  truths:
    - "_MCSL_WORKFLOW_GUIDE contains Auto-Generate Label flow (Actions menu → Generate Label → Label Batch page)"
    - "_MCSL_WORKFLOW_GUIDE contains Return Label flow (Actions menu → Create Return Label → Return Label modal → Submit → Return Created status)"
    - "test_auto_generate_flow passes — guide content verified to contain Actions menu + Label Batch pattern"
    - "test_return_label_flow passes — guide content verified to contain Create Return Label + Submit + Return Created"
  artifacts:
    - path: "pipeline/smart_ac_verifier.py"
      provides: "_MCSL_WORKFLOW_GUIDE with Auto-Generate and Return Label sections"
    - path: "tests/test_label_flows.py"
      provides: "test_auto_generate_flow and test_return_label_flow active (unskipped)"
  key_links:
    - from: "_MCSL_WORKFLOW_GUIDE"
      to: "_PLAN_PROMPT"
      via: "mcsl_workflow_guide= format arg — agent reads guide to decide nav_clicks"
      pattern: "Create Return Label"
    - from: "Return Label section"
      to: "existing_fulfilled order_action"
      via: "guide note: REQUIRES already-fulfilled order — use order_action: existing_fulfilled"
      pattern: "existing_fulfilled"
---

<objective>
Add Auto-Generate Label (LABEL-02) and Return Label (LABEL-04) flows to _MCSL_WORKFLOW_GUIDE and activate their tests.

Purpose: These two flows use the Actions menu pattern (header checkbox + Actions button + search menu) rather than the Order Summary button. Adding them to the workflow guide ensures the agent selects the correct nav path when these flows appear in a plan.

Output: _MCSL_WORKFLOW_GUIDE extended with two new sections; test_auto_generate_flow and test_return_label_flow active and passing.
</objective>

<execution_context>
@/Users/madan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/madan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-label-docs-pre-requirements/03-RESEARCH.md
@.planning/phases/03-label-docs-pre-requirements/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Exact locators confirmed from mcsl-test-automation POM files (Research doc Pattern 6 + 7) -->

Auto-Generate Label Flow (LABEL-02):
```
1. navigate: "orders"
2. Create order (order_action: create_new)
3. Filter by Order ID (Add filter → Order Id → fill → Escape)
4. Click header checkbox label: appFrame.getByRole("row", name="#").locator("label").first()
5. Click Actions button: appFrame.locator('div[class="buttons-row"] > button:nth-child(4)')
6. In Actions search → type "Generate Label" → click menuitem "Generate Label"
   → Label Batch page opens (same page as bulk)
7. Wait for SUCCESS status in batch → Click "Mark as Fulfilled"
```

Return Label Flow (LABEL-04):
```
REQUIRES: already-fulfilled order (order_action: existing_fulfilled)
1. navigate: "orders" → filter by Order ID
2. Click header checkbox label (same as above)
3. Click Actions button (same CSS selector)
4. In Actions search → type "Create Return Label" → click menuitem
5. Return Label modal opens:
   header locator: appFrame.locator("body > div:nth-child(15) > div > div:nth-child(1) > div > div > h3")
   → text = "Return Label"
6. Click Submit button: appFrame.getByRole("button", name="Submit")
7. Verify order status in grid = "Return Created"
   locator: appFrame.locator("#all-order-table table tbody tr").first().locator("td").nth(10)
```

From pipeline/smart_ac_verifier.py (current _MCSL_WORKFLOW_GUIDE):
```python
# "Bulk Labels" section already exists (lines 432-437) — Auto-Generate is a variant of this
# Return Label section does NOT exist yet — must be added
# _PLAN_PROMPT order judgment table may need "return label" → existing_fulfilled mapping
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auto-Generate Label flow in _MCSL_WORKFLOW_GUIDE + test</name>
  <files>pipeline/smart_ac_verifier.py, tests/test_label_flows.py</files>
  <behavior>
    - test_auto_generate_flow (unskip): assert _MCSL_WORKFLOW_GUIDE string contains "Actions button" AND "Generate Label" in the Actions menu context AND "Label Batch" page reference
    - The test is a content assertion on the guide string — no mock Claude needed
  </behavior>
  <action>
    RED: Unskip test_auto_generate_flow in tests/test_label_flows.py. Assert that _MCSL_WORKFLOW_GUIDE contains: "Actions button" (or "buttons-row > button:nth-child(4)"), "Generate Label" as an Actions menu item, and "Label Batch". Run test — it must fail (section not in guide yet).

    GREEN: Add an "Auto-Generate Label Flow" section to _MCSL_WORKFLOW_GUIDE in pipeline/smart_ac_verifier.py. Place it after the existing "Bulk Labels" section. Content:

    ```
    ### Auto-Generate Label Flow (single order via Actions menu)
    1. Click "ORDERS" tab → All Orders grid
    2. Add filter: Order Id → type order ID → press Escape
    3. Click header checkbox (getByRole "row" name="#" → locator "label" → first)
    4. Click Actions button (div[class="buttons-row"] > button:nth-child(4))
    5. In the Actions search box, type "Generate Label" → click menuitem "Generate Label"
       → Label Batch page opens (same as Bulk Labels)
    6. Wait for SUCCESS status in batch grid
    7. Click "Mark as Fulfilled"
    ```

    Commit sequence:
    1. test(03-02): unskip test_auto_generate_flow — assert guide contains Actions menu pattern
    2. feat(03-02): add Auto-Generate Label section to _MCSL_WORKFLOW_GUIDE
  </action>
  <verify>
    <automated>cd /Users/madan/Documents/MCSLDomainExpert && PYTHONPATH=. .venv/bin/pytest tests/test_label_flows.py::test_auto_generate_flow -x -q</automated>
  </verify>
  <done>test_auto_generate_flow passes; _MCSL_WORKFLOW_GUIDE contains Auto-Generate section with Actions menu steps</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Return Label flow in _MCSL_WORKFLOW_GUIDE + test</name>
  <files>pipeline/smart_ac_verifier.py, tests/test_label_flows.py</files>
  <behavior>
    - test_return_label_flow (unskip): assert _MCSL_WORKFLOW_GUIDE contains "Create Return Label", "Return Label" modal reference, "Submit" button, and "Return Created" status
    - Also assert guide contains a note that Return Label requires an already-fulfilled order
  </behavior>
  <action>
    RED: Unskip test_return_label_flow. Assert guide contains "Create Return Label", "Submit", "Return Created", and "existing_fulfilled" or "already-fulfilled". Run — must fail.

    GREEN: Add "Return Label Flow" section to _MCSL_WORKFLOW_GUIDE after the Auto-Generate section:

    ```
    ### Return Label Flow (LABEL-04)
    ⚠️ REQUIRES an already-FULFILLED order. Use order_action: existing_fulfilled in plan.
    1. Click "ORDERS" tab → All Orders grid
    2. Add filter: Order Id → type order ID → press Escape
    3. Click header checkbox (same as Auto-Generate)
    4. Click Actions button (div[class="buttons-row"] > button:nth-child(4))
    5. In Actions search → type "Create Return Label" → click menuitem "Create Return Label"
       → Return Label modal opens (header text = "Return Label")
    6. Click Submit button
    7. Verify order row status column = "Return Created"
       (locator: #all-order-table table tbody tr:first → td:nth-child(11))
    ```

    Also add "return label" → existing_fulfilled mapping to the order judgment table in _PLAN_PROMPT (search for "order_action" table in _PLAN_PROMPT and add a row: return label → existing_fulfilled).

    Commit sequence:
    1. test(03-02): unskip test_return_label_flow — assert guide contains Return Label pattern
    2. feat(03-02): add Return Label section to _MCSL_WORKFLOW_GUIDE + _PLAN_PROMPT order table
  </action>
  <verify>
    <automated>cd /Users/madan/Documents/MCSLDomainExpert && PYTHONPATH=. .venv/bin/pytest tests/test_label_flows.py::test_auto_generate_flow tests/test_label_flows.py::test_return_label_flow -x -q</automated>
  </verify>
  <done>Both tests pass; Return Label section in guide includes existing_fulfilled note; full suite green</done>
</task>

</tasks>

<verification>
PYTHONPATH=. .venv/bin/pytest tests/ -q — all active tests pass
grep "Create Return Label" pipeline/smart_ac_verifier.py — confirms section in guide
grep "existing_fulfilled" pipeline/smart_ac_verifier.py — confirms order table entry
</verification>

<success_criteria>
- _MCSL_WORKFLOW_GUIDE contains Auto-Generate Label section with Actions menu locator reference
- _MCSL_WORKFLOW_GUIDE contains Return Label section with "existing_fulfilled" warning
- test_auto_generate_flow and test_return_label_flow active and passing
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/03-label-docs-pre-requirements/03-02-SUMMARY.md`
</output>
