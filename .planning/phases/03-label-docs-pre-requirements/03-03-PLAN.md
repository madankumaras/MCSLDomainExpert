---
phase: 03-label-docs-pre-requirements
plan: "03"
type: tdd
wave: 2
depends_on:
  - 03-01
files_modified:
  - pipeline/smart_ac_verifier.py
  - tests/test_label_flows.py
autonomous: true
requirements:
  - LABEL-03

must_haves:
  truths:
    - "_MCSL_WORKFLOW_GUIDE contains Bulk Label flow: header checkbox → 'Generate labels' (lowercase l) → Label Batch page → SUCCESS → Mark as Fulfilled"
    - "_PLAN_PROMPT order judgment table maps 'bulk label' scenarios to order_action: create_bulk"
    - "test_bulk_label_flow passes — guide content contains header checkbox step, lowercase 'Generate labels', and Label Batch reference"
  artifacts:
    - path: "pipeline/smart_ac_verifier.py"
      provides: "_MCSL_WORKFLOW_GUIDE with Bulk Label section; _PLAN_PROMPT order table with bulk mapping"
    - path: "tests/test_label_flows.py"
      provides: "test_bulk_label_flow active and passing"
  key_links:
    - from: "_MCSL_WORKFLOW_GUIDE Bulk Label section"
      to: "_PLAN_PROMPT"
      via: "mcsl_workflow_guide= format arg"
      pattern: "Generate labels"
    - from: "_PLAN_PROMPT order judgment table"
      to: "create_bulk_orders()"
      via: "order_action: create_bulk in plan JSON"
      pattern: "create_bulk"
---

<objective>
Add Bulk Label generation flow (LABEL-03) to _MCSL_WORKFLOW_GUIDE and activate its test.

Purpose: Bulk label generation uses a distinct UI path — the header checkbox in the orders grid followed by the "Generate labels" button (lowercase l is critical). Without this section in the guide, the agent would attempt the wrong nav path for bulk scenarios.

Output: _MCSL_WORKFLOW_GUIDE extended with Bulk Label section; test_bulk_label_flow active and passing.
</objective>

<execution_context>
@/Users/madan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/madan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-label-docs-pre-requirements/03-RESEARCH.md
@.planning/phases/03-label-docs-pre-requirements/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Bulk label flow confirmed from orderGridPage.ts (Research Pattern 5) -->

Bulk Label Flow (LABEL-03):
```
1. navigate: "orders" (ORDERS tab, All Orders grid)
2. Create N orders (order_action: create_bulk, typically 3)
3. Click header checkbox to select all: appFrame.getByRole("row", name="#").locator("label").first()
4. Click "Generate labels" button (LOWERCASE "l" is exact):
   appFrame.get_by_role("button", name="Generate labels")
   → Label Batch page opens
5. Poll for SUCCESS status in batch grid (up to 300s):
   appFrame.get_by_role("cell", name="SUCCESS")
6. Click "Mark as Fulfilled": appFrame.get_by_role("button", name="Mark as Fulfilled")
```

CRITICAL: "Generate labels" uses lowercase "l". The exact string in orderGridPage.ts is:
  `role=button[name="Generate labels"]`
Using "Generate Labels" (capital L) will fail — click strategy falls through all 7 attempts.

From pipeline/smart_ac_verifier.py:
```python
# Existing "Bulk Labels" section in _MCSL_WORKFLOW_GUIDE (lines 432-437) — may be a stub
# Phase 3 replaces with full step sequence including the lowercase-l warning
# _PLAN_PROMPT order judgment table: "bulk label" → create_bulk (add if missing)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bulk Label flow in _MCSL_WORKFLOW_GUIDE + test</name>
  <files>pipeline/smart_ac_verifier.py, tests/test_label_flows.py</files>
  <behavior>
    - test_bulk_label_flow (unskip): assert _MCSL_WORKFLOW_GUIDE string contains "Generate labels" (lowercase l), "header checkbox", and "Label Batch" (or "label batch")
    - Also assert guide contains "Mark as Fulfilled" as the final bulk step
    - Test is a content assertion on the guide string — no mock Claude needed
  </behavior>
  <action>
    RED: Unskip test_bulk_label_flow in tests/test_label_flows.py. Assert _MCSL_WORKFLOW_GUIDE contains "Generate labels" (with lowercase l), the header row checkbox step, and "Label Batch". Run — must fail if section is stub or missing the exact casing.

    GREEN: Update the existing "Bulk Labels" section in _MCSL_WORKFLOW_GUIDE (or add a new one if absent). Replace or expand to:

    ```
    ### Bulk Label Flow (LABEL-03)
    1. Click "ORDERS" tab → All Orders grid
    2. Create multiple orders (order_action: create_bulk, default 3)
    3. Click the header row checkbox to select all filtered orders:
       (getByRole "row" name="#" → locator "label" → first click)
    4. Click "Generate labels" button — NOTE: lowercase "l" is EXACT, not "Generate Labels"
       → Label Batch page opens
    5. Wait for all rows to show SUCCESS status (poll up to 300s)
    6. Click "Mark as Fulfilled"
    ```

    Also ensure _PLAN_PROMPT order judgment table includes: "bulk label" → order_action: create_bulk. Search for the order judgment/action table in _PLAN_PROMPT and add this row if not present.

    Commit sequence:
    1. test(03-03): unskip test_bulk_label_flow — assert lowercase Generate labels + Label Batch
    2. feat(03-03): expand Bulk Label section in _MCSL_WORKFLOW_GUIDE with correct casing note
  </action>
  <verify>
    <automated>cd /Users/madan/Documents/MCSLDomainExpert && PYTHONPATH=. .venv/bin/pytest tests/test_label_flows.py::test_bulk_label_flow -x -q</automated>
  </verify>
  <done>test_bulk_label_flow passes; "Generate labels" (lowercase l) appears in _MCSL_WORKFLOW_GUIDE; full suite green</done>
</task>

</tasks>

<verification>
PYTHONPATH=. .venv/bin/pytest tests/ -q — all active tests pass
grep -n "Generate labels" pipeline/smart_ac_verifier.py — confirms lowercase "l" in guide
grep -n "create_bulk" pipeline/smart_ac_verifier.py — confirms order table entry
</verification>

<success_criteria>
- _MCSL_WORKFLOW_GUIDE contains Bulk Label section with "Generate labels" (lowercase l) explicitly noted
- _PLAN_PROMPT order table maps "bulk label" scenario → order_action: create_bulk
- test_bulk_label_flow active and passing
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/03-label-docs-pre-requirements/03-03-SUMMARY.md`
</output>
