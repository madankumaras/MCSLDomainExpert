---
phase: 05-full-dashboard-ui
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - pipeline_dashboard.py
  - tests/test_dashboard.py
autonomous: false
requirements:
  - UI-01
  - UI-05

must_haves:
  truths:
    - "App renders 7 tabs in exact order: User Story, Move Cards, Release QA, History, Sign Off, Write Automation, Run Automation"
    - "All 7 tab variable names (tab_us, tab_devdone, tab_release, tab_history, tab_signoff, tab_manual, tab_run) are present in source"
    - "Each tab body contains a stub st.info() with 'Coming in Phase N.' message"
    - "Pipeline header renders as pipeline-header CSS block above tabs"
    - "All 14 previously-passing tests still pass; test_ui01_tab_stubs and test_ui01_seven_tabs also pass"
  artifacts:
    - path: "pipeline_dashboard.py"
      provides: "Complete main() function: connection checks, code_paths guard, sidebar, pipeline header, 7-tab layout with stub bodies"
      exports:
        - "all Phase-4 functions preserved"
        - "_status_badge"
  key_links:
    - from: "st.tabs([...])"
      to: "7 tab context manager variables"
      via: "tab_us, tab_devdone, tab_release, tab_history, tab_signoff, tab_manual, tab_run = st.tabs([...])"
      pattern: "tab_us"
    - from: "pipeline-header div"
      to: ".pipeline-header CSS class in _CSS"
      via: "st.markdown(pipeline_header_html, unsafe_allow_html=True)"
      pattern: "pipeline-header"
---

<objective>
Complete `pipeline_dashboard.py` by adding the pipeline header block and the 7-tab layout with stub bodies. This is the final plan of Phase 5 — after this plan the file is a fully structured 7-tab app that can be launched with `streamlit run pipeline_dashboard.py`.

Purpose: Delivers UI-01 (7-tab layout) and UI-05 (branding header above tabs). Each tab body is a stub `st.info()` message pointing to the implementing phase.
Output: Updated `pipeline_dashboard.py` with full main() function; all 16 tests green (14 passing + 2 newly unskipped = 16 passing, 0 skipped, 0 failed).
</objective>

<execution_context>
@/Users/madan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/madan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-full-dashboard-ui/05-RESEARCH.md
@.planning/phases/05-full-dashboard-ui/05-01-SUMMARY.md
@.planning/phases/05-full-dashboard-ui/05-02-SUMMARY.md
@pipeline_dashboard.py
@tests/test_dashboard.py
</context>

<interfaces>
<!-- 7-tab layout — exact variable names required by test_ui01_tab_stubs -->
```python
# Source: FedexDomainExpert/ui/pipeline_dashboard.py line 904 pattern — adapted for MCSL
tab_us, tab_devdone, tab_release, tab_history, tab_signoff, tab_manual, tab_run = st.tabs([
    "📝 User Story",
    "🔀 Move Cards",
    "🚀 Release QA",
    "📋 History",
    "✅ Sign Off",
    "✍️ Write Automation",
    "▶️ Run Automation",
])
```

<!-- Pipeline header HTML block — uses .pipeline-header CSS class from _CSS -->
```python
st.markdown(
    '<div class="pipeline-header">'
    '<h1>🚚 MCSL QA Pipeline</h1>'
    '<p>AI-powered QA orchestration for MCSL carriers</p>'
    '</div>',
    unsafe_allow_html=True,
)
```

<!-- Tab stub bodies — one st.info() per tab -->
```python
with tab_us:
    st.info("User Story generation coming in Phase 6.")

with tab_devdone:
    st.info("Move Cards coming in Phase 6.")

with tab_release:
    st.info("Release QA pipeline coming in Phase 7.")

with tab_history:
    st.info("Pipeline history coming in Phase 6.")

with tab_signoff:
    st.info("Sign Off tab coming in Phase 8.")

with tab_manual:
    st.info("Write Automation coming in Phase 9.")

with tab_run:
    st.info("Run Automation coming in Phase 10.")
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pipeline header and 7-tab layout with stub bodies to main()</name>
  <files>pipeline_dashboard.py</files>
  <behavior>
    - st.tabs() called with exactly 7 labels in exact order: "📝 User Story", "🔀 Move Cards", "🚀 Release QA", "📋 History", "✅ Sign Off", "✍️ Write Automation", "▶️ Run Automation"
    - All 7 tab variables assigned: tab_us, tab_devdone, tab_release, tab_history, tab_signoff, tab_manual, tab_run
    - Each tab body has an st.info() stub (no empty with blocks)
    - Pipeline header div with class="pipeline-header" rendered before tabs
    - test_ui01_tab_stubs passes: all 7 variable names found in inspect.getsource(pd)
    - test_ui01_seven_tabs passes: module has _CSS attribute
    - All 14 previously-passing tests still pass
  </behavior>
  <action>
    In pipeline_dashboard.py, inside the `def main()` function, after the `with st.sidebar:` block, add:

    1. **Pipeline header** (renders above tabs):
       ```python
       st.markdown(
           '<div class="pipeline-header">'
           '<h1>🚚 MCSL QA Pipeline</h1>'
           '<p>AI-powered QA orchestration for MCSL carriers</p>'
           '</div>',
           unsafe_allow_html=True,
       )
       ```

    2. **7-tab layout**:
       ```python
       tab_us, tab_devdone, tab_release, tab_history, tab_signoff, tab_manual, tab_run = st.tabs([
           "📝 User Story",
           "🔀 Move Cards",
           "🚀 Release QA",
           "📋 History",
           "✅ Sign Off",
           "✍️ Write Automation",
           "▶️ Run Automation",
       ])

       with tab_us:
           st.info("User Story generation coming in Phase 6.")

       with tab_devdone:
           st.info("Move Cards coming in Phase 6.")

       with tab_release:
           st.info("Release QA pipeline coming in Phase 7.")

       with tab_history:
           st.info("Pipeline history coming in Phase 6.")

       with tab_signoff:
           st.info("Sign Off tab coming in Phase 8.")

       with tab_manual:
           st.info("Write Automation coming in Phase 9.")

       with tab_run:
           st.info("Run Automation coming in Phase 10.")
       ```

    3. Ensure the module-level run block at the bottom calls `main()`. The correct Streamlit pattern is:
       ```python
       # At module top-level (FIRST streamlit calls):
       st.set_page_config(...)      # already from 05-01
       st.markdown(_CSS, ...)       # already from 05-01

       # Then:
       _init_state()
       main()
       ```
       `st.set_page_config()` and `st.markdown(_CSS)` must remain the very first Streamlit calls at module top level.

    After writing, unskip `test_ui01_tab_stubs` and `test_ui01_seven_tabs` in tests/test_dashboard.py (remove the @pytest.mark.skip decorators from both).
  </action>
  <verify>
    <automated>PYTHONPATH=/Users/madan/Documents/MCSLDomainExpert/.claude/worktrees/objective-archimedes /Users/madan/Documents/MCSLDomainExpert/.venv/bin/pytest tests/test_dashboard.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <done>
    test_ui01_tab_stubs and test_ui01_seven_tabs both PASS.
    All 16 tests pass, 0 skipped, 0 failed.
    "tab_us", "tab_devdone", "tab_release", "tab_history", "tab_signoff", "tab_manual", "tab_run" all present in module source.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — 7-tab app launches and renders correctly in browser</name>
  <files>pipeline_dashboard.py</files>
  <action>
    Run the app and visually confirm the MCSL-branded 7-tab layout. No code changes are required in this task.
    Launch command: /Users/madan/Documents/MCSLDomainExpert/.venv/bin/streamlit run /Users/madan/Documents/MCSLDomainExpert/.claude/worktrees/objective-archimedes/pipeline_dashboard.py
  </action>
  <what-built>
    Complete pipeline_dashboard.py: MCSL-branded 7-tab Streamlit app with System Status badges, Release Progress sidebar, Knowledge Base expanders, Dry Run toggle, and placeholder tabs.
  </what-built>
  <how-to-verify>
    1. Start the app: `cd /Users/madan/Documents/MCSLDomainExpert/.claude/worktrees/objective-archimedes && /Users/madan/Documents/MCSLDomainExpert/.venv/bin/streamlit run pipeline_dashboard.py`
    2. Confirm the browser shows:
       - Header: "🚚 MCSL QA Pipeline" in dark teal-navy gradient banner
       - 7 tabs in correct order: User Story | Move Cards | Release QA | History | Sign Off | Write Automation | Run Automation
       - Sidebar System Status with 5 badges (Claude API, Trello, Slack, Google Sheets, Ollama)
       - Sidebar Release Progress section showing "Load a release in Release QA to see progress." (no release loaded yet)
       - Sidebar 3 KB expanders: Automation Code, Backend/Server Code, Frontend/Client Code
       - Sidebar Dry Run toggle at bottom
       - Each tab shows the "Coming in Phase N." info message
    3. Click each tab — confirm it switches without error
    4. Click "🧪 Automation Code" expander — confirm it opens with a path text input
    5. Press Ctrl+C to stop the app
  </how-to-verify>
  <verify>
    <automated>PYTHONPATH=/Users/madan/Documents/MCSLDomainExpert/.claude/worktrees/objective-archimedes /Users/madan/Documents/MCSLDomainExpert/.venv/bin/pytest tests/test_dashboard.py -q 2>&1 | tail -3</automated>
  </verify>
  <done>Human confirms all 5 visual checks pass; app launches without errors and all 7 tabs render stub content.</done>
  <resume-signal>Type "approved" if all 5 checks pass, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
Full test suite:
PYTHONPATH=/Users/madan/Documents/MCSLDomainExpert/.claude/worktrees/objective-archimedes /Users/madan/Documents/MCSLDomainExpert/.venv/bin/pytest tests/test_dashboard.py -v 2>&1 | tail -20

Expected: 16 passed, 0 skipped, 0 failed.

Source integrity checks:
grep -c "tab_us\|tab_devdone\|tab_release\|tab_history\|tab_signoff\|tab_manual\|tab_run" pipeline_dashboard.py
# Expected: >= 7 matches (one declaration line + 7 with blocks)

grep "BACKEND_CODE_PATH\|FRONTEND_CODE_PATH" pipeline_dashboard.py
# Expected: no output
</verification>

<success_criteria>
- pipeline_dashboard.py has a complete main() function: connection checks, code_paths guard, sidebar, pipeline header, 7-tab layout
- All 7 tab variable names present in source (confirmed by test_ui01_tab_stubs)
- Tabs labelled exactly: "📝 User Story", "🔀 Move Cards", "🚀 Release QA", "📋 History", "✅ Sign Off", "✍️ Write Automation", "▶️ Run Automation"
- Each tab body has a Coming-in-Phase-N stub
- All 16 tests pass, 0 skipped, 0 failed
- App launches in browser via streamlit run pipeline_dashboard.py without errors
- Human verifier approves visual appearance
</success_criteria>

<output>
After completion, create `.planning/phases/05-full-dashboard-ui/05-03-SUMMARY.md`
</output>
